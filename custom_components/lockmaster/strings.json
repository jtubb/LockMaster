{
  "config": {
    "step": {
      "user": {
        "title": "Set up LockMaster",
        "description": "Select the Zigbee locks to manage. Found {lock_count} lock(s) in zigbee2mqtt. All selected locks will share the same user/PIN configuration.",
        "data": {
          "locks": "Locks",
          "lock_name": "Lock Name"
        }
      }
    },
    "error": {
      "invalid_lock_name": "Please select a valid lock"
    },
    "abort": {
      "already_configured": "LockMaster is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "LockMaster Options",
        "menu_options": {
          "add_lock": "Add a lock",
          "remove_lock": "Remove a lock"
        }
      },
      "add_lock": {
        "title": "Add Locks",
        "description": "Select additional locks to manage. They will share the same user/PIN configuration.",
        "data": {
          "locks": "Locks"
        }
      },
      "remove_lock": {
        "title": "Remove Lock",
        "description": "Select a lock to remove.",
        "data": {
          "lock_name": "Lock"
        }
      }
    },
    "error": {
      "invalid_lock_name": "Please enter a valid lock name",
      "lock_already_exists": "This lock is already configured"
    },
    "abort": {
      "no_locks": "No locks configured",
      "no_locks_available": "No additional locks found in zigbee2mqtt"
    }
  },
  "entity": {
    "sensor": {
      "enabled_users": {
        "name": "Enabled Users"
      },
      "available_slots": {
        "name": "Available Slots"
      },
      "total_users": {
        "name": "Total Users"
      },
      "managed_locks": {
        "name": "Managed Locks"
      }
    }
  },
  "services": {
    "add_lock": {
      "name": "Add Lock",
      "description": "Add a new lock to manage.",
      "fields": {
        "lock_name": {
          "name": "Lock Name",
          "description": "The name of the lock as it appears in zigbee2mqtt."
        }
      }
    },
    "fetch_users": {
      "name": "Fetch Users",
      "description": "Fetch user data from all managed locks."
    },
    "refresh_users": {
      "name": "Refresh Users",
      "description": "Run consistency check and update Home Assistant entities."
    },
    "update_user": {
      "name": "Update User",
      "description": "Update a user's information and PIN.",
      "fields": {
        "user_id": {
          "name": "User ID",
          "description": "The user slot ID (0-29)."
        },
        "name": {
          "name": "Name",
          "description": "The user's name."
        },
        "email": {
          "name": "Email",
          "description": "The user's email address."
        },
        "pin": {
          "name": "PIN",
          "description": "The PIN code (4-8 digits)."
        }
      }
    },
    "disable_user": {
      "name": "Disable User",
      "description": "Disable a user and remove their PIN from all locks.",
      "fields": {
        "user_id": {
          "name": "User ID",
          "description": "The user slot ID to disable."
        }
      }
    },
    "generate_temp_user": {
      "name": "Generate Temporary User",
      "description": "Create a temporary user with a random PIN.",
      "fields": {
        "name": {
          "name": "Name",
          "description": "The temporary user's name."
        },
        "email": {
          "name": "Email",
          "description": "The temporary user's email address."
        },
        "reservation_start": {
          "name": "Start Time",
          "description": "When access should begin."
        },
        "reservation_end": {
          "name": "End Time",
          "description": "When access should end."
        }
      }
    }
  }
}
